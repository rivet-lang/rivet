fn main() {
    let a = 1;
    let a = 1; // fail

    let b = 1;
    let b = 1; // fail
}

let X = 1;
let X = 1; // fail

let global = 1;

fn function(x: int) {
    let x = 1; // fail

    let b = 1;
    {
        let b = 1; // fail
    }

    let global = 1; // fail
}

fn my_fn(a: int, b: int) {} // ok

fn win(a: int, a: int) {} // fail

fn dup() {}
fn dup() {} // fail
