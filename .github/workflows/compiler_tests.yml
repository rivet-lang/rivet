name: Compiler Tests

on:
  push:
    paths: ['src/weldc/**.v', '.github/workflows/compiler_tests.yml', 'tests/**']
  pull_request:
    paths: ['src/weldc/**.v', '.github/workflows/compiler_tests.yml', 'tests/**']
    types: [opened, synchronize]

jobs:
  ubuntu-gcc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build V
        run: |
          git clone https://github.com/vlang/v
          cd v && make && sudo ./v symlink
          cd ..

      - name: Build Weld compiler
        run: |
          make build

      - name: Run Weld compiler tests
        run: |
          make test
