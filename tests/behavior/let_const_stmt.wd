fn other_fn() {
    let bc = true;
}

const MAX_SIZE = 123;
const ARR_SIZE: int = 2;

let mut x = 1;
let static_abc = 1; // immutable

fn main() {
    let j = 1;
    let abc = (11 + 15) / 99;

    fn inside() {
        let inside_var = false;
    }

    let a9; // initted later
    a9 = 1;

    let some;
    some = "";

    let t, mut y, z;
}
